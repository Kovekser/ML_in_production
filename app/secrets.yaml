default:
  env: local
  minio:
    host: localhost
    port: 9000
    access_key: minio
    secret_key: minio123
test:
  env: test
  minio:
    host: 127.0.0.1
    port: 9000
    access_key: miniotest
    secret_key: miniotest